# Xcode / generated
# Bedrock.xcodeproj is generated by XcodeGen — never commit; regenerate with `xcodegen generate`
Bedrock.xcodeproj/
*.xcworkspace/xcuserdata/
xcuserdata/
DerivedData/
build/
*.xcuserstate

# Swift / SwiftPM
.build/
Packages/
*.resolved

# macOS
.DS_Store

# Node / backend
node_modules/
.next/
*.log

# Secrets (Supabase / RevenueCat / Railway keys — never commit)
Secrets.xcconfig
.env
*.env
!.env.example
