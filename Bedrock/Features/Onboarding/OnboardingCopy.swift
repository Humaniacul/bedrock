import Foundation

/// Statistics and social proof shown in onboarding.
///
/// ⚠️ VERIFY BEFORE LAUNCH — every value here is a placeholder. Shipping invented
/// figures risks App Store rejection and erodes trust. Replace each with a real,
/// defensible number (with a source you can cite) or remove the beat that uses it.
enum OnboardingStat {
    /// Act II — "you're not alone." Prevalence framing.
    static let prevalence = "1 in 3 men"

    /// Act II — neural-reset window used in the timeline + projection.
    static let resetDays = "90 days"

    /// Act IV — the movement / community size.
    static let communitySize = "40,000+"

    /// Act IV — a representative testimonial (replace with a real, consented one).
    static let testimonial = "“I got six years back. Bedrock is the first thing that ever held.”"
    static let testimonialAttribution = "— A.M., 211 days"
}
